aws --endpoint-url=http://localhost:4566 dynamodb scan --table-name ImageMetadata | python3 -c "
import sys, json
from decimal import Decimal
from boto3.dynamodb.types import TypeDeserializer

d = TypeDeserializer()
items = json.load(sys.stdin).get('Items', [])

def handle_decimals(obj):
    if isinstance(obj, Decimal):
        return int(obj) if obj % 1 == 0 else float(obj)
    raise TypeError

print(json.dumps([{k: d.deserialize(v) for k, v in i.items()} for i in items], indent=2, default=handle_decimals))
"